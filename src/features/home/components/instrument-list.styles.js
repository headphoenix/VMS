import styled from "styled-components/native";
import { Animated, FlatList } from "react-native";

export const InstrumentList = styled(Animated.FlatList).attrs({
  contentContainerStyle: {
    padding: 16,
  },
})``;